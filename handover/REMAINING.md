# Remaining outcomes for a dependable v1

These are requirements and evidence gaps, not coding prescriptions. Severity/findings come from the 8 September code audit; cloud failure mechanisms below were not reproduced in Spark. No production source changed after that audit during the handover.

| Priority / backlog | What remains and why | Completion evidence |
| --- | --- | --- |
| P0 F09 / A01 | Silver must reject missing required schema/null keys and handle conflicting duplicate business keys explicitly. Current key filtering/dropDuplicates can collapse order lines when LineNumber is absent. | Executed adversarial data fixtures; valid fact grain retained and bad input fails clearly. |
| P0 F10–F11 / A02 | Gold must validate persisted data, Date relationships, fact grain and every published aggregate. Failed validation must not leave existing model readers consuming partial/invalid published tables. Current code writes tables before final validation. | Independent numeric reconciliation, failure/publication cases and validated reuse of existing dependencies. |
| P0 F12 / A07 | Uploads must identify a complete dataset and isolate interrupted/rerun data. Shared Files/raw can mix old/new data; manifest-only input currently qualifies. | Interrupted upload, mixed-format/rerun and incomplete-input cases cannot be consumed as a valid new dataset. |
| P1 F01 / A06 | Local runs need durable dataset identity and failure/concurrency isolation. The adapter gate ends before upload; another generation can replace the shared output folder. | Repeated, concurrent, cancelled and failed runs cannot overwrite or publish the dataset another action consumes. |
| P1 F02, F24–F25 | Complete project/input validation and truthful seed/order wording; investigate very low orders relative to days and date boundaries; record reference input provenance. | Boundary tests and real output/count checks; UI/docs agree with actual supported behavior. No claim of arbitrary seed or exact row count. |
| P1 F03–F05 / A03–A04 | Enforce preflight/dependencies at execution boundaries, exact selected-stage effects, consistent rejection of duplicate item names, bounded auth/HTTP/polling and truthful timeout/cancel/unknown outcomes. | Runner/transport fault tests including hung operations, ambiguous targets and forbidden upstream effects. Current tests do not exercise these paths. |
| P1 F06–F07 / A05 | Durable evidence for every action, including standalone Prepare/Upload, partial provisioning and failed/current jobs; responsive UI and honest recovery/cancellation status. Current receipts save at start/end and miss some partial IDs. | Crash/partial-failure/write-failure evidence and actual WPF interaction. Active legacy generation currently finishes before cancellation is reported. |
| P1 F08 | Reproducible local smoke and appropriate CI branch/path coverage. Current workflow push filter only names the old desktop branch. | Portable repeat-run evidence and CI for the actual candidate; historical temp harness alone does not close this. |
| P1 F13–F14 | Tiny SalesBi must work end to end in a real capacity-backed workspace, including all validation tables, accepted TMDL/PBIR, measure queries, relationships, rendered visuals and same-name rerun behavior. | Real service/job IDs, reconciliation results, model query results, rendered report and successful deliberate rerun. Local factory assertions are insufficient. |
| P1 F17, F26 | Complete supported-path logic audit and practical Windows distribution. Unchanged generator formulas/writers and full BI semantics have not received comprehensive review. | Recorded correctness coverage plus clean-machine launch/prerequisite/user journey evidence. |
| Hardening F15–F16 | CSV/Delta path coverage, measured larger scales, recovery/retention and ownership-safe cleanup. | Format/scale matrix, recovery cases and clear ownership boundaries; no unsupported speed or scale promises. |

External prerequisites: a designated capacity-backed Fabric workspace, access/authentication and permitted artifact prefix. Azure CLI was not found on PATH on 11 September. No live mutation is authorized for an unspecified target. There is no credible final completion date until local correctness and the live vertical slice are proven.

Later product decisions, not mandatory scope expansion: native sign-in, reporting-currency conversion policy, richer reports, CustomerExperience/DataQuality/MlDissatisfaction scenarios, workspace/capacity provisioning. Those scenario enum values are catalog entries, not implemented features. The original handoff discusses these ambitions; the dependable SalesBi v1 is the current target.
