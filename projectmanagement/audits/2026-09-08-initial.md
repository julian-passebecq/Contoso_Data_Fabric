# Initial tech-lead review — 2026-09-08

Decision: approve S01 implementation, not release or sprint acceptance.

Scope: inspected Core planning/project/generation, Fabric runner/receipts/API/upload,
generated notebooks and relevant semantic-model/desktop paths; examined existing tests,
CI, architecture docs and working-tree changes. This is a focused architecture and logic
review, not an exhaustive audit of every original generator formula or report definition.

Candidate: `feat/csharp-fabric-desktop-v1` at
`47a65b75e0ec126c0c8a0372a1c27709659aa317` plus pre-existing uncommitted stabilization changes.
See the source hash inventory and dirty-file record in ../evidence/.

Executed now:

- `dotnet build ContosoDGV2.sln -c Release --no-restore`: PASS, 0 warnings, 0 errors,
  MSBuild elapsed 49.82 seconds. Existing restored assets were used; restore was not rerun.
- `dotnet test ContosoFabric.Core.Tests/ContosoFabric.Core.Tests.csproj -c Release --no-build --no-restore --logger "console;verbosity=normal"`:
  PASS, 31/31, reported test-run time 2.8713 seconds. Expected engine failure/log output
  appeared inside a passing regression test; it was not a failed smoke run.
- No real generation, WPF interaction, Fabric tenant operation, remote fetch, or CI run
  was executed in this planning session.

Delegated light-model support was read-only: test/branch/toolchain inventory. Lead code
findings and acceptance decisions remain independent of that inventory.

## Findings

| ID | Severity | Code evidence and failure mechanism | Disposition |
| --- | --- | --- | --- |
| A01 | P0 release blocker | [Silver factory](../../ContosoFabric.Fabric/Notebooks/NotebookDefinitionFactory.cs), lines 97–100, filters requested keys down to existing columns and calls dropDuplicates. With sales missing LineNumber, multiple lines sharing OrderKey can collapse; conflicting payloads with identical keys have no deterministic survivor policy. This is a code-path inference, not an executed Spark reproduction. | F09, S02; schema/key fixtures before extending scenarios |
| A02 | P0 release blocker | [Gold factory](../../ContosoFabric.Fabric/Notebooks/NotebookDefinitionFactory.cs), lines 135–148 and 197–252, writes published tables before final validation. A failed run can already have changed data read by an existing model. Row/aggregate reconciliation compares constructed dataframes; Date FK/grain/null checks and other aggregate reconciliation are absent. Blocking subsequent model deployment cannot roll back existing writes. | F10/F11, lead publication design in S02 |
| A03 | P1 | [Catalog client](../../ContosoFabric.Fabric/Api/FabricRestClient.cs), lines 116–117 and 155–156, chooses FirstOrDefault for same-name Lakehouse/notebook; models/reports and dependencies elsewhere reject duplicates. Two same-name targets can mutate an arbitrary item. | F04, S01 P2; fault fixture with duplicate names |
| A04 | P1 | [REST client](../../ContosoFabric.Fabric/Api/FabricRestClient.cs), lines 26, 203, 260 and 295–302; [definition client](../../ContosoFabric.Fabric/Api/FabricDefinitionDeployer.cs), lines 15, 136 and 170–177: infinite HttpClient timeout and loop-only deadlines cannot bound a hung token acquisition/request. A total operation can exceed its apparent limit. | F05, S01 P2; fake hung token/request and deadline tests |
| A05 | P1 | [Receipt](../../ContosoFabric.Fabric/Pipeline/PipelineRunReceipt.cs) saves at start/finalization. [Runner](../../ContosoFabric.Fabric/Pipeline/FabricPipelineRunner.cs) records provisioning after all Prepare calls return and jobs only after completion. Crash, partial preparation or failed current job loses useful IDs; standalone Prepare/Upload lack receipts. | F06, S01 P3; persist effect-level evidence |
| A06 | P1 | [Adapter](../../ContosoFabric.Core/Generation/LegacyGeneratorAdapter.cs) releases the process gate after manifest creation, while [runner](../../ContosoFabric.Fabric/Pipeline/FabricPipelineRunner.cs) uploads shared generated/data afterward. Another adapter run can replace it; [engine setup](../../DatabaseGenerator/Engine_Setup.cs), lines 31–47, deletes known files at generation start. UI busy handling reduces single-window overlap but is not a dataset identity contract. | F01, S01 P1/P2; immutable local datasets and fixed per-run reference |
| A07 | P0 release blocker | [Uploader](../../ContosoFabric.Fabric/OneLake/OneLakeRawUploader.cs) writes files independently into shared Files/raw and accepts any supported candidate; even a manifest-only directory qualifies. There is no dataset completion barrier, and legacy uploads can coexist with old manifest/data. Runner invocation does not enforce the separate UI preflight check. | Local completeness F01/F03 in S01; remote landing F12 in S02 |
| A08 | P1 evidence gap | Existing 31 cases do not exercise runner effects, transport or actual Spark semantics; no current live model/report evidence. Some README/native architecture paragraphs still cite 24 tests or promise an exact order count, alongside newer corrected notes. | F02/F03/F08, S01; F13/F14 live gates later |

Additional investigation F24: [Engine constructor](../../DatabaseGenerator/Engine.cs) computes
its order-number daily range using integer OrdersCount / totalDays before Log10. Planner
allows very small order counts. Reproduce boundary behavior; do not claim corruption was
observed or change the original algorithm without an oracle.

Reviewed stabilization assessment: disposing the logger in Engine.Exec finally and holding
the adapter semaphore through manifest creation address the previously reproduced locked-log
failure mechanism. Worker-thread execution removes the obvious synchronous WPF preparation
path. Current tests support log reuse/failure disposal and receipt serialization/error safety;
they do not prove real adapter concurrency, interactive responsiveness or all cancellation races.

Historical evidence from [stabilization notes](../../docs/GENERATION_STABILIZATION_2026-09-08.md):
two real warm Tiny/one-year/Parquet runs reportedly matched actual 9,466 orders and 22,704
order rows, with elapsed 46.76 and 35.16 seconds. Treat those as documented prior results;
S01 must put an equivalent repeatable smoke harness in the repo. The earlier readiness
audit describes a blocker before those fixes, not the current observed test result.

Required next review: after medium implementation and light testing, inspect the actual
complete S01 diff, including these inherited changes. Audit dataset commit/cancellation
races, transport deadline/cancellation distinction, forbidden stage effects, and receipt
write ordering. A green test suite alone is insufficient for acceptance.

## Audit coverage to close before v1

F26 tracks the full supported SalesBi path, including unchanged code. Sprint reviews must
record what was read and what remains; a reviewed diff alone cannot close this feature.

| Area | Current evidence boundary | Planned lead review |
| --- | --- | --- |
| Legacy generator and writers | Lifetime and selected setup/key logic inspected; business formulas and all writers not comprehensively audited | S02: order/line grain, sales mapping, quantity/discount/net-price/cost meaning, dates, RNG contract, CSV/Parquet/Delta output consistency |
| Core project/generation | Focused planning/validation/adapter/manifest inspection | S01: full contract, dataset publication and failure/concurrency paths |
| Fabric orchestration/transport | Focused implementation inspection with findings above | S01: complete effect sequencing, identity, time bounds, cancellation and diagnostics |
| Bronze/Silver/Gold | Generated code inspected; actual Spark execution absent | S02: business invariants and executed adversarial fixtures, publication protocol |
| Model/report | Relevant relationship/currency paths and existing contract coverage inspected | S03: complete supported model/measure/report logic plus service/query/render evidence |
| WPF/distribution | Relevant handlers inspected; interactive and clean-machine behavior absent | S01 interaction evidence; S04 distribution/recovery review |

The light tester can map data columns, enumerate assertions and collect reproductions.
The lead owns interpretation of financial/data meaning and signs off the integrated path.
